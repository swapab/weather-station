defaults: &defaults
  adapter: postgresql

development:
  <<: *defaults
  database: weather-station-dev

test:
  <<: *defaults
  database: weather-station-test

production:
  <<: *defaults
  database: weather-statation-prod
